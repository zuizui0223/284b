name: same-target-heldout-crosscheck-DEPRECATED

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse legacy prediction-opening path
        run: |
          echo "Deprecated: use same-target-heldout-crosscheck-strict. Held-out prediction cells may open only when both answers are adequate and the matching successor reference ceiling is frozen."
          exit 1
