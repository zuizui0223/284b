name: same-target-successor-pairing-calibration-DEPRECATED

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse legacy prediction-opening path
        run: |
          echo "Deprecated: use same-target-successor-pairing-calibration-strict after the pre-discordance reference-feasibility audit."
          exit 1
