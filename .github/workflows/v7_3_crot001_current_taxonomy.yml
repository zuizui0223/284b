name: v7-3-crot001-current-taxonomy

on:
  push:
    branches:
      - product-b-v5-obligate-invariant
    paths:
      - .github/workflows/v7_3_crot001_current_taxonomy.yml

permissions:
  contents: read

jobs:
  taxonomy-only:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Verify repository tests before current taxonomy access
        env:
          PYTHONPATH: .
        run: python -m unittest discover -s tests -v
      - name: Resolve CROT001 against current GBIF taxonomy only
        env:
          PYTHONPATH: .
        run: python scripts/run_v7_3_crot001_current_taxonomy.py
      - name: Upload CROT001 current taxonomy audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: product-b-v7-3-crot001-current-taxonomy
          path: artifacts/product_b_v7_3_crot001_current_taxonomy.json
          if-no-files-found: error
          retention-days: 90
