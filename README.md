# 284b

## Active scientific outputs

284b is now split into two clean active tracks.

### 1. Level-A empirical short report

The submission-facing paper asks whether independently fitted species-distribution answers from preserved specimens and human observations reproduce on fresh taxa when the target, comparison geometry and evaluation rule are frozen prospectively.

Current endpoint:

- 12 independent held-out taxa
- 288 prespecified diagnostics
- 283 evaluable / 5 unresolved
- 0/12 taxa with a frozen-envelope exceedance
- closest observed/envelope ratio = 0.988

See `manuscript/LEVEL_A_SHORT_REPORT_V0_1.md`.

### 2. Evidence-boundary structural result

A separate positive methodological result is kept active:

- collapsing invalid states into biological zero inflates false violations by exactly `1-a1`;
- the corresponding apparent sensitivity gain is exactly `1-a0`;
- when validity is equal across classes, both increments are `1-a`;
- passing a measurement gate does not itself control false-discovery fraction.

See `docs/evidence_boundary_structural_result.md`.

This result is not part of the Level-A short report.

## Legacy boundary

Unresolved Level-B/Level-C candidate programs, earlier candidate hunting, unopened focal dependency endpoints, and the old flagship submission package are preserved under `legacy/pre_level_a_focus/`.

Legacy means inactive, not deleted. Files moved there remain available for provenance and later reuse.

## Active Level-A evidence

- `docs/product_b_same_target_core19_empirical_result.md`
- `results/product_b_same_target_core19_v0_4_heldout_final_receipt.json`
- `results/reviewer2_level_a_structure_audit_v0_1.json`

Historical utility modules that the same-target pipeline still imports remain at their original package paths so the active reconstruction and CI remain executable.
